--@ olmadan mail adresi kabul etmemek
ALTER TABLE ogrenci
add constraint CHC_Mail2  CHECK(CHARINDEX('@',email)>0)

--@ olmadan mail adresi kabul etmemek
ALTER TABLE personel
add constraint CHC_Mail  CHECK(CHARINDEX('@',email)>0)


--@ olmadan mail adresi kabul etmemek
ALTER TABLE yönetici
add constraint CHC_Mail1  CHECK(CHARINDEX('@',email)>0)
